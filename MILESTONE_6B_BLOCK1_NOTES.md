# Milestone 6B Block 1 Notes

This block starts M6B by migrating RingCentral and Less Annoying CRM records onto the shared source-control pipeline introduced in M6A.

It adds a connector-neutral migration layer that reads canonical source-control pipeline artifacts and emits migration packs, review queues, and rollups for RingCentral/LACRM records only.
